apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: kafka-connect-build
spec:
  source:
    type: Git
    git:
      uri: https://github.com/seu-usuario/seu-repo.git
      ref: main
    contextDir: .
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: kafka-connect-oracle-cdc:latest
